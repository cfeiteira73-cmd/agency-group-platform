'use client'

// ── OFF-MARKET MODAL — REMOVIDO ───────────────────────────────────────────────
// Modal "Acesso Restrito / Portfolio Off-Market" removido definitivamente.
// Sem overlay, sem listeners, sem estados, sem exit intent, sem timers.
// Retorna null — zero DOM output em qualquer dispositivo.
export default function HomeModals() {
  return null
}
